# In codegen.yml
schema: "server/src/graphql/schemas/**/*.graphql"

generates:
  server/src/__generated__/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      useIndexSignature: true